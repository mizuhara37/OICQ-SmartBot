const logger = require('../util/logger.js');
const config = require('../config.json');
const LocalStorage = require('node-localstorage').LocalStorage;
class exam
{
    constructor (client) {
       this.client = client;
    }

    activate () {
       logger.info('Exam组件加载成功！');
    }
    async onGroupMessage (session)
    {
      
    }
}
module.exports = exam;